import 'package:flutter/material.dart';

class Descritiontab extends StatelessWidget {
  const Descritiontab({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Description"),),
    );
  }
}